# Quick access overlay terminal (toggle with:
#   open -na kitty.app --args +kitten quick-access-terminal btop)

# Centred floating window, sized by lines/columns
edge center-sized
lines 30
columns 120

background_opacity 0.85

# Hide automatically when you click away
hide_on_focus_loss yes
